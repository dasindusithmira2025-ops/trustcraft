node_modules/
dist/
build/
.cache/
.env*
.vite/
.firebase/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Flutter / Dart
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
.pub-cache/
.pub/
coverage/
**/android/local.properties
**/android/key.properties
**/android/**/GeneratedPluginRegistrant.java
**/ios/Flutter/ephemeral/
**/ios/Pods/
**/ios/.symlinks/
**/DerivedData/

# Android Studio / Gradle
.gradle/
.idea/
*.iml
**/.cxx/
**/captures/
*.jks
*.keystore

# VS Code local state (project tasks/config are intentionally tracked)
.vscode/*.log
.vscode/.ropeproject

# Native build / QA output
*.apk
*.aab
*.ipa
screenshots/
qa-artifacts/

# Credentials and local operating-system data
*.pem
*.p12
*.mobileprovision
*.credentials
.DS_Store
Thumbs.db
desktop.ini

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
