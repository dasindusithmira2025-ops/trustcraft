node_modules/
dist/
build/
.cache/
.env*
.vite/
.firebase/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*

# Editor local state
.vscode/*.log
.vscode/.ropeproject
.idea/
*.iml

# QA output
screenshots/
qa-artifacts/

# Credentials and local operating-system data
*.pem
*.p12
*.credentials
.DS_Store
Thumbs.db
desktop.ini

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
